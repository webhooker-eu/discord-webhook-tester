services:
  discord-webhook-tester:
    build: .
    image: discord-webhook-tester
    restart: unless-stopped
    ports:
      # Published on localhost only: the form has no authentication.
      - "127.0.0.1:${PORT:-8080}:8080"
